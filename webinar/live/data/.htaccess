# Runtime data (chat logs, presence heartbeats) must never be readable
# over the web — access goes through chat.php / presence.php only.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
